import { StatsCompilation } from 'webpack';
declare type FormatWebpackMessages = (json: StatsCompilation) => {
    errors: string[];
    warnings: string[];
};
declare const formatWebpackMessages: FormatWebpackMessages;
export default formatWebpackMessages;
