import * as T from './types';
/**
 * Icestore class
 *
 * an instance of Icestore generated by "init"
 */
export default class Icestore {
    protected config: T.Config;
    protected models: T.Model[];
    private plugins;
    private pluginFactory;
    constructor(config: T.Config);
    forEachPlugin(method: string, fn: (content: any) => void): void;
    getModels: (models: T.Models) => T.Model[];
    addModel(model: T.Model): void;
    init(): any;
}
