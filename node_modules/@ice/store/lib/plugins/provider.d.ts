import React from 'react';
import * as T from '../types';
interface ProviderConfig {
    context: React.Context<null>;
}
declare const _default: ({ context }: ProviderConfig) => T.Plugin;
export default _default;
