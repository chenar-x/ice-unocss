import * as T from '../types';
interface ReduxHooksConfig {
    context: any;
}
declare const _default: ({ context }: ReduxHooksConfig) => T.Plugin;
/**
 * Redux Hooks Plugin
 *
 * generates redux hooks for store
 */
export default _default;
