import * as T from '../types';
export interface ErrorConfig {
    name?: string;
    whitelist?: string[];
    blacklist?: string[];
    asNumber?: boolean;
}
interface IErrorState {
    error: Error;
    value: boolean;
}
export interface ErrorState<M extends T.Models> {
    error: {
        global: IErrorState;
        models: {
            [modelName in keyof M]: IErrorState;
        };
        effects: {
            [modelName in keyof M]: {
                [effectName in keyof T.ExtractIModelDispatchersFromEffects<M[modelName]['effects']>]: IErrorState;
            };
        };
    };
}
declare const _default: (config?: ErrorConfig) => T.Plugin;
export default _default;
