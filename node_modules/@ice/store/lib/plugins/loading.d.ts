import * as T from '../types';
export interface LoadingConfig {
    name?: string;
    whitelist?: string[];
    blacklist?: string[];
    asNumber?: boolean;
}
export interface LoadingState<M extends T.Models> {
    loading: {
        global: boolean;
        models: {
            [modelName in keyof M]: boolean;
        };
        effects: {
            [modelName in keyof M]: {
                [effectName in keyof T.ExtractIModelDispatchersFromEffects<M[modelName]['effects']>]: boolean;
            };
        };
    };
}
declare const _default: (config?: LoadingConfig) => T.Plugin;
export default _default;
