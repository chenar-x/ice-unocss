import * as T from '../types';
/**
 * validate
 *
 * takes an array of arrays of validations and
 * throws if an error occurs
 */
declare const validate: (validations: T.Validation[]) => void;
export default validate;
