import * as T from '../types';
declare const _default: (initConfig: T.InitConfig & {
    name: string;
}) => T.Config;
/**
 * mergeConfig
 *
 * merge init configs together
 */
export default _default;
