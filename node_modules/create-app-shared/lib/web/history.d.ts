import type { CreateHistory, InitHistory } from '../createInitHistory';
declare const createHistory: CreateHistory;
declare const initHistory: InitHistory;
export { initHistory };
export default createHistory;
