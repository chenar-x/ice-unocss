import { History, Location } from 'history';
import { AppConfig } from './types';
interface Route {
    path?: string;
    source: string;
    [key: string]: unknown;
}
export declare type CreateHistory = (options: {
    type?: string;
    basename?: string;
    location?: Location;
    routes?: Route[];
    customHistory?: History;
}) => History<unknown>;
declare type InitialContext = null | {
    location?: Location;
};
declare type Options = {
    initialContext?: InitialContext;
    staticConfig?: {
        routes: Route[];
        [key: string]: unknown;
    };
};
export declare type InitHistory = (appConfig: AppConfig, options?: Options) => void;
declare const _default: (createHistory: CreateHistory) => (appConfig: AppConfig, options?: Options) => void;
export default _default;
