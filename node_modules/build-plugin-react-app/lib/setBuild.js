var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var HtmlWebpackPlugin = require('@builder/pack/deps/html-webpack-plugin');
var _a = require('./utils'), getWebOutputPath = _a.getWebOutputPath, logWebpackConfig = _a.logWebpackConfig;
module.exports = function (api) {
    var context = api.context, onHook = api.onHook, onGetWebpackConfig = api.onGetWebpackConfig;
    onHook('before.build.run', function (_a) {
        var config = _a.config;
        logWebpackConfig(config);
    });
    onGetWebpackConfig(function (config) {
        var outputPath = getWebOutputPath(context);
        config.output.path(outputPath);
        // make a copy of html-webpack-plugin to generate 404.html
        if (config.plugins.get('HtmlWebpackPlugin')) {
            config
                .plugin('HtmlWebpackPlugin_404')
                .use(HtmlWebpackPlugin, [__assign(__assign({}, config.plugin('HtmlWebpackPlugin').get('args')[0]), { filename: '404.html' })]);
        }
    });
};
//# sourceMappingURL=setBuild.js.map