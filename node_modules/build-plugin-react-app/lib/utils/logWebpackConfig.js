var debug = require('debug')('icejs');
module.exports = function (config) {
    try {
        var tmp_1 = [];
        debug(JSON.stringify(config, function (key, val) {
            if (val != null && typeof val === 'object') {
                if (tmp_1.indexOf(val) >= 0) {
                    return;
                }
                tmp_1.push(val);
            }
            return val;
        }, 2));
    }
    catch (error) {
        // ignore error
    }
};
//# sourceMappingURL=logWebpackConfig.js.map