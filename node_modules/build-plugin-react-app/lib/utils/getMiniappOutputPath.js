var fs = require('fs-extra');
var path = require('path');
var MINIAPP = require('../constants').MINIAPP;
module.exports = function (context, options) {
    if (options === void 0) { options = {}; }
    var rootDir = context.rootDir, userConfig = context.userConfig;
    var outputDir = userConfig.outputDir;
    var output = path.resolve(rootDir, outputDir);
    fs.ensureDirSync(output);
    return path.resolve(output, options.target || MINIAPP);
};
//# sourceMappingURL=getMiniappOutputPath.js.map