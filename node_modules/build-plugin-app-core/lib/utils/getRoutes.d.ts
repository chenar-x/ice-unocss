interface IParams {
    rootDir: string;
    tempPath: string;
    configPath: string;
    projectType: string;
    isMpa: boolean;
    srcDir: string;
}
interface IResult {
    routesPath: string;
    isConfigRoutes: boolean;
}
declare function getRoutes({ rootDir, tempPath, configPath, projectType, isMpa, srcDir }: IParams): IResult;
export default getRoutes;
