"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var formatPath_1 = require("./formatPath");
// entry: src/app -> srcDir: src
// entry: client/app -> srcDir: client
// mpa entry: {dashboard: 'src/pages/Dashboard/app.ts'} -> src
function getSourceDir(entry) {
    var entryStr = 'src/app';
    if (typeof entry === 'string') {
        entryStr = entry;
    }
    else if (typeof entry === 'object') {
        var values = Object.values(entry);
        if (typeof values[0] === 'string') {
            entryStr = values[0];
        }
    }
    entryStr = (0, formatPath_1.default)(entryStr);
    return entryStr.split('/')[0];
}
exports.default = getSourceDir;
//# sourceMappingURL=getSourceDir.js.map