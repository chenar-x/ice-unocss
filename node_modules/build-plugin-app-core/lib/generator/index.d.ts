import { IExportData } from '../types/base';
declare type IRenderDataFunction = (renderDataFunction: IRenderData) => IRenderData;
declare type IRenderData = Record<string, unknown>;
declare type IExtraData = IRenderData | IRenderDataFunction;
interface IRegistration {
    [key: string]: any[];
}
interface IRenderFile {
    (templatePath: string, targetDir: string, extraData?: IExtraData): void;
}
interface IRenderDataRegistration {
    (renderDataFunction: IRenderData): IRenderData;
}
interface ITemplateOptions {
    template: string;
    targetDir: string;
}
export default class Generator {
    targetDir: string;
    renderData: IRenderData;
    contentRegistration: IRegistration;
    private rerender;
    private rootDir;
    private renderTemplates;
    private renderDataRegistration;
    private log;
    private showPrettierError;
    private disableRuntimePlugins;
    private plugins;
    constructor({ rootDir, targetDir, defaultData, log, plugins }: {
        rootDir: any;
        targetDir: any;
        defaultData: any;
        log: any;
        plugins: any;
    });
    addExport: (registerKey: any, exportData: IExportData | IExportData[]) => void;
    removeExport: (registerKey: string, removeExportName: string | string[]) => void;
    addContent(apiName: any, ...args: any[]): void;
    private getExportStr;
    parseRenderData(): {
        staticConfig: string;
        globalStyle: string;
        entryImportsBefore: string;
        entryImportsAfter: string;
        entryCodeBefore: string | any[];
        entryCodeAfter: string | any[];
    };
    generateImportStr(apiName: string): string;
    render: () => void;
    debounceRender: any;
    addRenderFile: (templatePath: string, targetPath: string, extraData?: IExtraData) => void;
    addTemplateFiles: (templateOptions: string | ITemplateOptions, extraData?: IExtraData) => void;
    modifyRenderData(registration: IRenderDataRegistration): void;
    renderFile: IRenderFile;
    addDisableRuntimePlugin: (pluginName: string) => void;
}
export {};
