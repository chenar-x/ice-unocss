"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var path = require("path");
var fse = require("fs-extra");
var constant_1 = require("../constant");
exports.default = (function (api, options) {
    var context = api.context, setValue = api.setValue;
    var rootDir = context.rootDir;
    var framework = options.framework;
    var isRax = framework === 'rax';
    var tempDir = isRax ? 'rax' : 'ice';
    var tempPath = path.join(rootDir, "." + (process.env.__FRAMEWORK_NAME__ || tempDir));
    setValue(constant_1.TEMP_PATH, tempPath);
    setValue(constant_1.ICE_TEMP, tempPath);
    fse.ensureDirSync(tempPath);
    fse.emptyDirSync(tempPath);
});
//# sourceMappingURL=setTempDir.js.map