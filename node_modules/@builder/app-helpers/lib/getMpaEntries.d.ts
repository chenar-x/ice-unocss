import { IEntry } from './types';
interface IOptions {
    target: string;
    appJsonPath?: string;
    appJsonContent?: string;
}
export default function (api: any, options?: IOptions): IEntry[];
export {};
