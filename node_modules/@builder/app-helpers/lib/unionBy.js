"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var merge = require('lodash').merge;
function default_1(array, key) {
    var result = {};
    array.forEach(function (item) {
        var target = item;
        if (result[item[key]]) {
            target = merge(result[item[key]], item);
            // Override defaultValue
            if (Object.prototype.hasOwnProperty.call(item, 'defaultValue')) {
                target.defaultValue = item.defaultValue;
            }
        }
        result[item[key]] = target;
    });
    return Object.keys(result).map(function (item) { return result[item]; });
}
exports.default = default_1;
//# sourceMappingURL=unionBy.js.map