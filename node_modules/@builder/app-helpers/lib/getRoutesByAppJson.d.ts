import { IRoute } from './types';
interface IStaticConfig {
    routes: IRoute[];
    window?: any;
    tabBar?: any;
}
interface IOptions {
    appJsonPath?: string;
    appJsonContent?: IStaticConfig;
}
export default function (target: any, { appJsonPath, appJsonContent }: IOptions): IRoute[];
export {};
