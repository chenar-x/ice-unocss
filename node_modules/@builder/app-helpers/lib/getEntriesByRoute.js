"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var path = require("path");
var fs = require("fs-extra");
exports.default = (function (route, rootDir) {
    var entries = [];
    var pageHeader = route.pageHeader, frames = route.frames;
    var rootEntry = getEntry(route, rootDir);
    if (rootEntry) {
        entries.push(rootEntry);
    }
    if (frames) {
        frames.forEach(function (frame, index) {
            var entry = getEntry(frame, rootDir);
            if (entry) {
                entry.__frameIndex = index;
                entries.push(entry);
            }
        });
    }
    if (pageHeader) {
        var entry = getEntry(pageHeader, rootDir);
        if (entry) {
            entry.__pageHeader = true;
            entries.push(entry);
        }
    }
    return entries;
});
function getEntry(route, rootDir) {
    var source = route.source, name = route.name, pageSource = route.pageSource;
    var entryName;
    if (name) {
        entryName = name;
    }
    else if (source) {
        var dir = path.dirname(source);
        entryName = path.parse(dir).name.toLocaleLowerCase();
    }
    if (entryName) {
        return __assign(__assign({}, route), { entryName: entryName, entryPath: pageSource || getEntryPath(rootDir, source) });
    }
}
function getEntryPath(rootDir, source) {
    var absolutePath = path.join(rootDir, 'src', source);
    var targetExt = ['ts', 'tsx', 'js', 'jsx'].find(function (ext) { return fs.existsSync(absolutePath + "." + ext); });
    if (!targetExt) {
        throw new Error("Cannot find target file " + absolutePath + ".");
    }
    return absolutePath + "." + targetExt;
}
//# sourceMappingURL=getEntriesByRoute.js.map