import { IPluginAPI } from 'build-scripts';
import { IGeneratorOptions, IRunAppRenderData } from '../types';
export default class BaseGenerator {
    entryFolder: string;
    rootDir: string;
    options: IGeneratorOptions;
    builtInMethods: {
        applyMethod: Function;
        getValue: Function;
        setValue: Function;
    };
    runAppPath: string;
    entryPath: string;
    targetDir: string;
    routesFilePath: string;
    disableRuntimeList: string[];
    runAppRenderData: IRunAppRenderData;
    constructor(api: IPluginAPI, options: IGeneratorOptions);
    generateRunAppFile(userConfig: any): void;
    generateEntryFile(): void;
    generateLoadRuntimeModules(routesFilePath: string): void;
    getRoutesFilePath(): string;
}
