var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
module.exports = function (config, babelPlugins, _a) {
    var userConfig = _a.userConfig;
    if (userConfig.swc)
        return;
    ['jsx', 'tsx'].forEach(function (rule) {
        config.module
            .rule(rule)
            .use('babel-loader')
            .tap(function (options) {
            var _a = options.plugins, plugins = _a === void 0 ? [] : _a;
            return __assign(__assign({}, options), { plugins: __spreadArray(__spreadArray([], plugins, true), babelPlugins, true) });
        });
    });
};
//# sourceMappingURL=babelPlugins.js.map