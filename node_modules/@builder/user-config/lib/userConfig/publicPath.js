var updateMiniCssLoaderPath = require('../utils/updateMiniCssLoaderPath');
module.exports = function (config, value, context) {
    var command = context.command, userConfig = context.userConfig;
    if (command === 'build') {
        config.output.publicPath(value);
        updateMiniCssLoaderPath(config, value, userConfig);
    }
};
//# sourceMappingURL=publicPath.js.map