import { Context, ITaskConfig } from 'build-scripts';
import { InlineConfig, BuildOptions } from 'vite';
declare type Option = BuildOptions & InlineConfig;
/**
 * 配置转化函数
 */
export declare const recordMap: (chain: ITaskConfig['chainConfig'], ctx: Context) => Partial<Record<keyof Option, any>>;
export {};
