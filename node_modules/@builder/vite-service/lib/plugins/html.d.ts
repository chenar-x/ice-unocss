import type { Plugin } from 'vite';
interface Option {
    filename: string;
    template: string;
    entry: string;
    rootDir: string;
    templateParameters?: object;
}
export declare const htmlPlugin: ({ filename, template, entry, rootDir, templateParameters }: Option) => Plugin;
export {};
