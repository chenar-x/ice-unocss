export declare const openssl: (args: string[], options?: any) => string;
/**
 * 新增的域名是否在已存在域名内，支持一级的通配符*，比如pub.alimama.com属于*.alimama.com
 */
export declare const isMatched: (existedDomains: string[], addedDomains: string[]) => boolean;
/**
 * 获取最终需要新增的域名
 */
export declare const getAdded: (existedDomain: string[], addedDomains: string[]) => string[];
